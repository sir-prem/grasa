class MouseClickedPoint {

    constructor(mouseX, mouseY) {
        this.x = mouseX;
        this.y = mouseY;
    }

    set(mouseX, mouseY) {
        this.x = mouseX;
        this.y = mouseY;
    }

    

}
